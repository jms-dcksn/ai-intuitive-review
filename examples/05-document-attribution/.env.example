# Only needed to REGENERATE the recorded analysis (scripts/generate-findings.mjs).
# The app itself replays lib/findings.json and needs no key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: model for the generation script (default: claude-opus-4-8).
# CITATIONS_MODEL=claude-opus-4-8
